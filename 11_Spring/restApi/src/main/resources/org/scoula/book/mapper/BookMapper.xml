<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.book.mapper.BookMapper">
    <insert id="create">
        insert into Book (title, author, available)
        values (#{title}, #{author}, #{available})

        <selectKey resultType="Long" keyProperty="no" keyColumn="no" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <update id="update">
        update Book
        set update_date = now()
        where no = #{id}
    </update>

    <delete id="delete">
        delete
        from Book
        where no = #{id}
    </delete>

    <select id="getList" resultType="BookDto">
    <![CDATA[
            select * from  Book
            order by no desc
        ]]>
    </select>

    <select id="get" resultType="BookDto">
        select * from Book where no = #{id}

    </select>

</mapper>